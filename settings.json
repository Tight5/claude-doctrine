{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|mcp__github__push_files|mcp__github__create_or_update_file|mcp__github__delete_file",
        "hooks": [
          { "type": "command", "command": "h=\"$HOME\"/.claude/hooks/deny-founder-gates.sh; [ -x \"$h\" ] && exec \"$h\"; echo \"DENIED by founder doctrine: wall script $h is missing, so this call cannot be checked (fail closed). Reinstall the doctrine: the session-start hook in a container, sync.sh on the founder's machine.\" >&2; exit 2" }
        ]
      },
      {
        "matcher": "mcp__Open_Brain__capture_thought|mcp__Open_Brain__park_note|mcp__Open_Brain__backfill_canonical_embeddings",
        "hooks": [
          { "type": "command", "command": "h=\"$HOME\"/.claude/hooks/deny-brain-write.sh; [ -x \"$h\" ] && exec \"$h\"; echo \"DENIED by founder doctrine: wall script $h is missing, so this call cannot be checked (fail closed). Reinstall the doctrine.\" >&2; exit 2" }
        ]
      }
    ]
  }
}
